/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath:"/saturation-tool",
    output: "export",  // <=== enables static exports
    reactStrictMode: true,
};

export default nextConfig;
